- name: Update server1 packages
  hosts: 192.168.157.133
  become: yes
  tasks:
    - name: Run apt update
      apt:
        update_cache: yes  # Update the apt package cache
        cache_valid_time: 3600  # Cache considered valid for 1 hour
